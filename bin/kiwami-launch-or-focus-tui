#!/bin/bash

# Launch or focus on a given TUI identified by the passed in as the command.

APP_ID="org.kiwami.$(basename "$1")"
LAUNCH_COMMAND="kiwami-launch-tui $@"

exec kiwami-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
